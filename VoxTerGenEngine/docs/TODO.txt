********* REFACTOR & OPTIMIZATIONS *********

!!! DOCUMENT ALL FUNCTIONS !!!

WorldRenderer::ProcessChunkMeshReady Make chunk_mesh_render_data a member variable and reuse the GPU buffers, not erase and allocate new.
study orthographic/pixel-coordinate UI approach
investigate why I need to do '-2' and '- 2' in Wireframe mesh
move ScreenDimensionsData somewhere so I dont have to include Application.hpp???
implement more sophisticated transparent render order
make wireframe of chunk look nicer
move functions like GetChunkCoords to math?
prioritize chunk streaming based on camera direction
make chunk frustum aabb bigger to avoid popping in/out
Chunk::Pos() ??
in headers, use forwarding declarations whenever possible instead of include
PACK vertex into uint32
rethink the world block query implicit passing of chunk! at the call site it's not clear at all which chunk is the current!
rethink error handling. For example in Chunk.cpp. There's BOTH throw AND LOG, or throw AND assert.
rework MathUtils
make classes MeshBuilder & CollisionSystem a namespace
move world_up into better nested namespace
use & fix GetChunkCoords - math_utils
start simplifying constructors, for example in Texture2D, move parameters that belong together into struct Texture2DSettings, Observer, etc
simplify Observer::RelativeBlockPos lol

add a proper templated Vec class into "VoxTerGenAlgorithms/utils/Math.hpp"
create a single templated class for opensimplex, they are the same headers only different names! 
	template <typename Variant>
	class OpenSimplexNoise
rethink hashing for noise, better math::DotGrad?

std::uint8_t -> Material in Mesh & Vertex?
cleanup thread classes includes

sort all #includes by alphabetical order and these order rules:
	// 1. Corresponding header
	// 2. Standard library
	// 3. Third-party libraries
	// 4. Project headers

********* GRAPHICS & OPENGL *********

Vertex pulling?, binary greedy meshing?, chunk object pooling?

interpolated_view?!
review the camera class thoroughly
verify the camera is working correctly for big floats
SDL_WINDOWEVENT_SIZE_CHANGED update camera projection matrix
VERY IMPORTANT, MAKE THE WINDOWS CORRECT SIZE BASED ON MONITOR SIZE SO IT DOES NOT CUTOFF LIKE THE MAZE GENERATOR ON MBA 13

glEnable(GL_FRAMEBUFFER_SRGB);
vsync
tick rate - 20/60?
opengl/glad correct version check when init, initialization of subsystems review
add Graphics Device Interface - move all graphics apis inits into it - App will own it along with engine

********* BUGS *********

transparent chunks (water) glitchy - ocasionally not transparent when next to a solid, or when 2 waters are next to each other on chunk borders! 16x1x16|16x1x16
face culling on the chunk borders!
fix bug when running very fast, I fall through the floor
fix bug when falling very fast, I fall through the floor

********* NOTES *********

Test with multiple compilers, GPU's, etc.!